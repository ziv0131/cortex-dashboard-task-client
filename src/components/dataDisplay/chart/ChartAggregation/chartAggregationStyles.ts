import { makeStyles } from '@mui/styles';

export const useChartAggregationStyles = makeStyles(() => ({
  aggregationSelectorArea: {
    width: '100%',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
  },
}));
